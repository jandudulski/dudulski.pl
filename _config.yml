source: ./_source
destination: ./_site

timezone: Europe/Warsaw

strict_front_matter: true

permalink: pretty

defaults:
  -
    scope:
      path: ""
    values:
      layout: default

plugins:
  - jekyll-feed
